var myApp = angular.module("app.map", []);


myApp.controller('MapController', function($scope, $location, urlService) {
    // Parameter Start
    // Parameter End

    // Function Start
    $scope.init = function() {
    };
    // Function End

    // Directive Start
    // Directive End
    $scope.init();
});
